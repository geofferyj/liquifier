# Production overrides — set sensitive values via APP__* env vars
application:
  environment: production

database:
  max_connections: 25

api_gateway:
  cors_allowed_origin: "https://app.example.com"

websocket:
  cors_allowed_origin: "https://app.example.com"
