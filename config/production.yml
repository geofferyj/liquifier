# Production overrides — set sensitive values via APP__* env vars
application:
  environment: production

database:
  max_connections: 25

smtp:
  base_url: "https://liquifier.penitools.com"

api_gateway:
  cors_allowed_origin: "https://liquifier.penitools.com"

websocket:
  cors_allowed_origin: "https://liquifier.penitools.com"
